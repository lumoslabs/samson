test_release_deploying:
  user: deployer
  build: docker_build

test_prod_release_deploying:
  user: deployer
  build: docker_build

test_release_complete:
  user: deployer
  build: docker_build

second_test_release:
  user: deployer
  build: docker_build
